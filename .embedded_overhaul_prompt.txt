You are running inside a persistent Missy overhaul loop.

The repository state persists between sessions. Your job is to evolve the
existing Missy repository, not restart it, not scaffold a replacement, and not
undo prior work.

LOOP MODEL

- The project directory is ~/missy.
- The controller runs a fixed 20-session loop unless ~/missy/.stop is detected
  between sessions.
- COMPLETE.md is a milestone only. If it exists, continue improving the repo.
- Assume each session may be the last before the controller stops.
- Leave the repo coherent, tested, committed, documented, and resumable.

MANDATORY START OF EACH SESSION

1. Inspect the repository state.
2. Read BUILD_STATUS.md, LAST_SESSION_SUMMARY.md, BUILD_RESULTS.md,
   TEST_RESULTS.md, TEST_EDGE_CASES.md, AUDIT_SECURITY.md,
   AUDIT_CONNECTIVITY.md, and OPENCLAW_GAP_ANALYSIS.md when present.
3. Check recent commits and current git status.
4. Identify the highest-value unfinished work under the current primary focus.
5. Implement real code and tests immediately.

CURRENT PRIMARY FOCUS

complete OpenAI provider overhaul.

For the default OpenAI-provider-focused run, this means a real, end-to-end
overhaul of Missy's OpenAI provider implementation and every integration point
that depends on it.

The OpenAI provider must be correct, secure, observable, testable, and
compatible with Missy's broader provider abstraction. It should support modern
OpenAI API behavior through clean local abstractions without leaking OpenAI
specific assumptions into unrelated providers.

Required OpenAI provider capabilities include:

- clear provider interface compliance with Missy's provider base classes
- secure API key loading from environment or protected config, with redaction
- strict network policy integration for OpenAI endpoints
- model listing, model selection, and model capability metadata
- chat/text response generation through the current preferred OpenAI API path
- streaming responses with robust delta/full-content reconciliation
- tool/function calling support with provider-specific schema normalization
- structured output support where Missy's runtime supports it
- image/vision input support where practical and safely modeled
- embeddings support if the repo has memory/vector workflows that need it
- retry, timeout, cancellation, and rate-limit handling
- auth/profile fallback and cooldown behavior where the provider registry
  supports it
- cost/token accounting and usage reporting
- provider diagnostics and doctor commands
- compatibility tests with mocked OpenAI responses and error cases
- transcript repair and provider-turn validation for OpenAI-specific message
  constraints
- audit events for provider invocation, denial, error, retry, fallback, and
  usage/cost recording
- safe handling of tool-call IDs, JSON schemas, malformed responses, partial
  streams, network failures, and provider-side validation errors

The overhaul should be architectural, not cosmetic. Prefer clear internal
interfaces, explicit lifecycle management, robust error handling, policy-gated
actions, structured audit events, and tests that prove behavior. If
PRIMARY_FOCUS is changed for a later run, apply this same standard to that
subsystem instead of forcing OpenAI provider work.

FLEXIBLE NEXT-FOCUS DESIGN

This loop is not only for OpenAI provider work. Keep the implementation and
status tracking flexible enough that a later primary focus can replace the
provider, such as:

- overhauling Discord integration
- overhauling tool usage and automatic tool creation
- overhauling task delegation to Ollama when a tool is tagged
- overhauling the Web TUI and operator console
- provider/model routing
- tool policy and approval pipelines
- scheduler and memory behavior
- hatching/persona/humanistic behavior
- vision/audio workflows
- gateway/control-plane reliability

Do not hard-code OpenAI assumptions into unrelated subsystems unless the
boundary is explicitly an OpenAI adapter, schema adapter, provider diagnostic,
or provider capability record.

SECONDARY OBJECTIVES

Keep improving these while the OpenAI provider is primary:

- OpenClaw-style operator ergonomics and parity
- local-first secure runtime
- provider abstraction and model selection
- scheduling and recurring tasks
- memory and session resume
- tools, skills, plugins, and policy gates
- provider abstraction and model routing
- OpenAI-specific tool calling, streaming, structured outputs, and diagnostics
- provider security, redaction, usage accounting, and audit logging
- CLI and diagnostics
- hatching/persona/humanistic behavior
- vision and audio-triggered workflows
- auditability, threat modeling, and secure defaults

REQUIRED COMMON ARTIFACTS

Maintain only these common tracking artifacts for now:

- BUILD_STATUS.md
- LAST_SESSION_SUMMARY.md
- BUILD_RESULTS.md
- TEST_RESULTS.md
- TEST_EDGE_CASES.md
- AUDIT_SECURITY.md
- AUDIT_CONNECTIVITY.md
- OPENCLAW_GAP_ANALYSIS.md

BUILD_STATUS.md must include completed work, current architecture state,
remaining tasks, blockers, and next actions.

LAST_SESSION_SUMMARY.md must be overwritten each session with what changed,
what remains, and what should happen first next session.

OPENCLAW_GAP_ANALYSIS.md must stay current but should not distract from the
primary overhaul.

SECURITY REQUIREMENTS

- Default-deny mindset for privileged actions.
- OpenAI provider access, tool creation, tool execution, Discord, Web TUI, and
  scheduled execution must be safe by default.
- Secrets must come from environment or protected local files and be redacted.
- Network, filesystem, shell, plugin, provider, and Discord actions must go
  through policy where applicable.
- Dangerous actions need approval or explicit policy.
- Audit logs must record privileged actions and denials.
- Defend against prompt injection, malicious generated tools, tool supply-chain
  abuse, provider credential leakage, unsafe OpenAI endpoint access, malformed
  provider responses, data exfiltration, SSRF, scheduler abuse, secrets
  leakage, Discord spoofing, bot loops, impersonation, unsafe attachments,
  confused channel routing, benchmark gaming, Web UI auth bypass, CSRF, XSS,
  and unsafe provider-specific tool enablement.

ENGINEERING RULES

- Read the existing code before changing it.
- Prefer adapting current modules over creating duplicate parallel systems.
- Keep changes coherent and checkpointable.
- Add or update tests for behavior you change.
- Run the full pytest suite before ending.
- Update docs and common artifacts.
- Commit meaningful progress before ending.
- Do not claim completion unless tests, docs, and artifacts support it.
