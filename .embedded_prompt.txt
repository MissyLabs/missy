You are running inside a persistent build loop environment.

A controller script invokes Claude multiple times to progressively build the project.

LOOP BEHAVIOR

The controller will run exactly 20 sessions unless a controller-side stop file is detected between sessions.

Each session may terminate after up to 100 turns.

After termination the controller starts another session until all 20 sessions are exhausted.

The repository state persists between sessions.

Your job is to continue progress across sessions.

You must inspect the repository and resume work intelligently.

Do NOT restart the project each session.

IMPORTANT:

The controller does NOT stop when COMPLETE.md exists.
It will still continue running until all 20 sessions are finished unless a controller-side stop file is detected between sessions.

Therefore:
- If COMPLETE.md already exists, do not stop
- Instead, use later sessions for hardening, bug fixing, deeper tests, better docs, parity review, refactors, and security improvements
- Treat COMPLETE.md as "baseline completion reached", not "session loop ends"

IMPORTANT STOP-FILE SEMANTICS

A controller-side stop file may exist at:

~/git/missy/.stop

That stop file is handled by the outer shell loop, not by you directly.

Your behavior requirement is:
- never intentionally leave the repository in a half-finished state
- assume the current session may be the last one before the controller exits
- before ending a session, always persist useful progress
- always update BUILD_STATUS.md with exact current state
- always leave recovery breadcrumbs for the next session
- prefer coherent, checkpointed progress over risky sprawling edits

SESSION OBJECTIVE

The dominant next-phase objective is to overhaul, harden, and fully operationalize Missy's vision system as a first-class subsystem while preserving the broader secure local agent platform goals.

The goal is to build Missy, a secure local agent system.

Missy must reach feature parity with:

https://github.com/openclaw/openclaw

but implemented as a clean-room Python redesign.

You may study OpenClaw architecture and features, but you must not copy code.

Instead you must reimplement equivalent capabilities.

Inspired by:

https://github.com/nagisanzenin/skyclaw
https://github.com/openclaw/openclaw

But implemented as a secure Python-native local platform.

--------------------------------------------------
PRIMARY NEW PRIORITY: ROBUST VISION SUBSYSTEM
--------------------------------------------------

Vision is now the dominant priority for the remaining build effort.

Missy must implement an extremely robust on-demand vision subsystem suitable for local use on a Linux host with a USB webcam and additional image sources.

Target camera:
- Logitech C922x
- connected via USB to the Linux machine running Missy

The implementation must be production-minded, resilient, diagnosable, testable, and usable by the rest of the system.

VISION CAPABILITIES REQUIRED

Missy must support all of the following image sources:

1. live USB webcam capture
2. still-image file ingest
3. screenshot ingest
4. saved photo review
5. short-session scene memory across a task

Missy must support all of the following interaction patterns:

1. explicit on-demand visual inspection
2. wake-word plus intent-triggered visual activation through existing audio channels
3. brief auto-activation when audio intent strongly implies vision is needed
4. image review as part of conversational task execution
5. visual state tracking across a multi-step user task

VISION IMPLEMENTATION RULES

The vision stack must prioritize robustness and device resilience.

Camera discovery must use:
- USB vendor/product ID and/or device name detection
- robustness to re-enumeration
- stable rediscovery logic when device paths change

Camera capture must use:
- OpenCV capture on the discovered device path

Do NOT default to raw V4L2 integration unless:
- OpenCV has a specific blocking limitation
- that limitation is documented
- the fallback is justified in writing

Missy must gracefully handle:
- no camera present
- device busy
- permission denied
- intermittent disconnect/reconnect
- bad frame reads
- blank frames
- low light
- unsupported resolution or format
- camera warm-up instability
- stale cached device paths

The vision subsystem must expose diagnostics, retries, logging, and clear user-facing error messages.

--------------------------------------------------
VISION TASK REQUIREMENTS
--------------------------------------------------

Missy must support robust assistance for tabletop puzzle tasks.

Puzzle assistance must go beyond simple captioning and should attempt:

- board-state tracking
- piece identification
- edge/corner detection
- piece grouping suggestions
- color/texture clustering
- likely region matching
- orientation hints
- step-by-step placement guidance
- multi-step scene memory during an active puzzle task

Missy must support art review for a painter's painting.

Painting feedback mode must provide:
- warm encouragement
- gentle praise
- calm teaching tone
- constructive composition feedback
- non-harsh critique
- supportive stylistic commentary
- confidence-building suggestions

This mode should feel inspired by the emotional style of a gentle painting coach:
- encouraging
- reassuring
- observant
- never cruel
- never dismissive
- never sterile or robotic

The goal is not mere image description; the goal is useful, emotionally appropriate assistance.

--------------------------------------------------
AUDIO-TRIGGERED VISION
--------------------------------------------------

Missy's existing audio channels must be able to trigger vision abilities.

Required behavior:

- wake word detection must remain separate from final task execution
- intent detection must decide whether the request likely needs visual input
- when visual input is strongly implied, Missy may briefly auto-activate vision
- auto-activation must remain scoped to the active task
- visual activation must not become a permanent always-on surveillance mode
- activation decisions, captures, and outcomes must be logged

Examples of vision-triggering intents include:
- "Missy, look at this"
- "Can you tell where this puzzle piece goes?"
- "What do you think of this painting?"
- "Can you check what is on the table?"
- "Does this piece fit in the sky section?"
- "How can I improve this painting?"

--------------------------------------------------
REQUIRED VISION SUBSYSTEM ARTIFACTS
--------------------------------------------------

You MUST create and maintain a first-class vision subsystem.

Required files and docs:

~/git/missy/VISION.md
~/git/missy/VISION_AUDIT.md
~/git/missy/VISION_TEST_PLAN.md
~/git/missy/VISION_DEVICE_NOTES.md

The codebase must include a clear subsystem for vision, such as modules under paths like:

missy/vision/
missy/channels/
missy/agent/
missy/cli/

Exact file structure may vary, but the subsystem must be clean and explicit.

Required capabilities:
- camera discovery
- capture pipeline
- image normalization/preprocessing
- source abstraction layer for webcam/file/screenshot/photo
- task-scoped scene memory
- request routing from audio intent to visual execution
- structured logging
- failure recovery and retries
- tests and fixtures where practical

Required CLI commands or equivalent user-facing entry points:

missy vision devices
missy vision capture
missy vision inspect
missy vision review
missy vision doctor

These commands must be real and useful, not placeholders.

Suggested meanings:
- devices: enumerate and diagnose available cameras
- capture: take a frame or task-scoped image set
- inspect: run general visual analysis
- review: run domain-specific analysis such as puzzle help or painting feedback
- doctor: test camera pipeline, permissions, discovery, and capture health

--------------------------------------------------
HATCHING + PERSONA SYSTEM
--------------------------------------------------

Missy must still implement and preserve an OpenClaw-style hatching process and persona system.

HATCHING DEFINITION:

Hatching is the initial bootstrapping experience where:
- the agent establishes identity
- the agent configures behavior patterns
- the agent initializes memory
- the agent becomes usable in a human-friendly way
- the agent validates major subsystems including vision, audio, and provider readiness

You MUST implement or maintain:

1. HATCHING SYSTEM

Create or maintain:

~/git/missy/HATCHING.md
~/git/missy/HATCHING_LOG.md

The system must include:
- first-run detection
- guided initialization flow
- automatic environment validation
- provider setup verification
- security baseline initialization
- initial memory seeding
- subsystem readiness checks
- vision readiness checks
- audio-to-vision trigger readiness checks

CLI command:

missy hatch

This command must:
- walk through setup
- create initial config
- generate persona
- validate system readiness
- validate vision subsystem presence and usability where possible
- clearly report what remains incomplete

2. PERSONA SYSTEM

Missy must have a structured persona model.

Create or maintain:

~/git/missy/PERSONA.md

Persona must include:
- name
- tone
- personality traits
- behavioral tendencies
- response style rules
- boundaries and constraints
- system identity description
- specialized mode guidance for supportive visual coaching

Persona must be:
- editable
- versioned
- loaded at runtime
- enforced during responses

CLI commands:

missy persona show
missy persona edit
missy persona reset

3. HUMANISTIC BEHAVIOR LAYER

Missy must behave in a way that feels:
- intuitive
- context-aware
- conversationally natural
- non-robotic
- adaptive to user intent

You MUST implement or maintain:
- response shaping layer
- tone adaptation
- context carryover
- memory-informed replies
- intent interpretation
- emotionally appropriate visual feedback modes

This should exist as a distinct subsystem such as:

missy/agent/behavior.py

Behavior rules must:
- avoid robotic repetition
- avoid empty flattery
- adapt to user tone
- prioritize usefulness and clarity
- simulate understanding rather than mechanical output
- provide encouraging painting feedback without becoming fake or overblown
- provide puzzle guidance in actionable steps

4. MEMORY + IDENTITY LINK

Persona must influence:
- memory storage
- response style
- decision-making
- how Missy frames visual feedback

Memory should track:
- user preferences
- interaction patterns
- prior context
- task-scoped scene memory for active visual work

--------------------------------------------------
PROGRESS TRACKING
--------------------------------------------------

Maintain the file:

~/git/missy/BUILD_STATUS.md

This file must include:
- Completed components
- Current architecture
- Remaining work
- Next steps for the next iteration
- Vision-specific status
- Device support status
- Audio-triggered vision status
- Known blockers
- Exact recovery notes if the next session must resume from partial work

Every session must update it.

Also maintain iteration notes in BUILD_STATUS.md describing what was improved in the current session even if the project is already complete.

CRASH RECOVERY

If the previous session crashed you must recover using:
- BUILD_STATUS.md
- existing repository code
- build_log.txt
- any vision audit artifacts already present

--------------------------------------------------
COMPLETION CONDITIONS
--------------------------------------------------

If the system becomes fully complete you may create:

~/git/missy/COMPLETE.md

Only create it when ALL are true:

- Feature parity with OpenClaw achieved or credibly documented with remaining deltas
- All critical subsystems implemented
- Hatching system implemented
- Persona system implemented
- Behavior layer implemented
- Vision subsystem implemented as a first-class subsystem
- Audio-triggered vision path implemented
- Scene memory implemented for active visual tasks
- Webcam discovery and capture work robustly
- Tests passing
- Security policies implemented
- Documentation written
- CLI functional
- Scheduler functional
- Providers functional
- Plugin system functional
- Policy engine functional

IMPORTANT AFTER COMPLETION

If COMPLETE.md already exists, continue improving the project for the remaining sessions by focusing on:
- vision robustness
- vision edge cases
- camera recovery logic
- puzzle-assistance quality
- painting-feedback quality
- persona refinement
- behavior improvements
- realism improvements
- refactors
- bug fixes
- extra tests
- fuzz or edge-case coverage
- security hardening
- documentation improvements
- better operational tooling
- release polish

--------------------------------------------------
REQUIRED REPORT FILES BEFORE OR BY COMPLETION
--------------------------------------------------

~/git/missy/BUILD_RESULTS.md
~/git/missy/AUDIT_SECURITY.md
~/git/missy/AUDIT_CONNECTIVITY.md
~/git/missy/TEST_RESULTS.md
~/git/missy/TEST_EDGE_CASES.md
~/git/missy/VISION.md
~/git/missy/VISION_AUDIT.md
~/git/missy/VISION_TEST_PLAN.md
~/git/missy/VISION_DEVICE_NOTES.md

--------------------------------------------------
SESSION PRIORITIES
--------------------------------------------------

Every session should prioritize:

1. Implementing real code
2. Committing progress
3. Updating BUILD_STATUS.md
4. Expanding tests
5. Improving security
6. Verifying feature parity against OpenClaw
7. Improving persona realism
8. Improving human-like behavior
9. Making vision more robust
10. Improving audio-triggered visual task routing
11. Improving diagnostics and operational usability

Prefer many small commits.

Never stop early if time remains.

PROJECT: MISSY

Missy is a local-first agentic assistant platform.

PRIMARY REQUIREMENTS

Missy must:
- Run locally on Linux
- Be fully written in Python
- Support OpenAI
- Support Anthropic
- Support Ollama
- Support tools
- Support skills
- Support plugins
- Support scheduling and recurring jobs
- Support memory
- Support channels
- Support structured vision capabilities
- Enforce strict security policies
- Maintain structured audit logs

SECURITY MODEL

Missy must enforce DEFAULT DENY network egress.

Allowed networks must support:
- CIDR allowlists
- Wildcard domains
- Exact hostnames

All outbound traffic must go through a policy network client.

Filesystem writes must be sandboxed.

Shell execution must be policy controlled.

Plugins must declare permissions.

User input must be treated as untrusted.

Every privileged action must be logged with:
- timestamp
- task id
- session id
- policy rule
- result

Vision-related privileged or sensitive actions must also be logged, including:
- device selected
- source type
- capture start/end
- trigger reason
- inference target
- success/failure state

THREAT MODEL

Document defenses against:
- Prompt injection
- Plugin abuse
- Data exfiltration
- SSRF
- Scheduler abuse
- Secrets leakage
- Tool abuse
- Channel impersonation
- unsafe or accidental visual activation
- confused audio-triggered activation

ARCHITECTURE

Create and maintain a Python project with modules such as:

missy/
  core/
  agent/
  gateway/
  providers/
  tools/
  skills/
  plugins/
  scheduler/
  memory/
  policy/
  channels/
  cli/
  config/
  observability/
  security/
  vision/
  tests/

FINAL EXPECTATION

Missy must be a secure production-grade local agent platform with:
- OpenClaw-level capability
- Stronger security
- Realistic persona
- Human-like interaction model
- Robust vision support
- audio-triggered task-aware visual assistance
- production-usable diagnostics and docs

At the start of each session:
1. Inspect the repository state
2. Read BUILD_STATUS.md if it exists
3. Continue from the current state
4. If COMPLETE.md exists, continue with hardening and parity verification work
5. Treat vision as the dominant next-phase priority
6. Update BUILD_STATUS.md before ending

Start by analyzing the repository state and continuing the implementation.
