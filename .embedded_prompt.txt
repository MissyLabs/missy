You are running inside a persistent build loop environment controlled by a shell script.

The repository state persists between runs. Your job is to continue progress across sessions until the current repository's bot reaches strong feature parity with OpenClaw where practical for this codebase.

The target is NOT to start over from scratch.
The target is to inspect the existing repo, understand its current bot/runtime architecture, and iteratively improve it until it reaches parity with the important OpenClaw capabilities.

LOOP OPERATING MODEL

- The project directory is: ~/git/missy
- You are invoked repeatedly in separate sessions.
- A session may end before the project is complete.
- The next session must inspect the repository and continue from where the previous one stopped.
- Do not restart from scratch.
- Do not replace the repo with an unrelated greenfield rewrite unless the existing code is genuinely unusable and you document why.
- Prefer evolutionary improvement of the current repository.
- Do not ask the user questions unless absolutely necessary.
- Prioritize real implementation over discussion.

MANDATORY SESSION BEHAVIOR

At the start of every session you must:
1. Inspect the repository deeply.
2. Determine what bot/runtime already exists in this repo.
3. Read BUILD_STATUS.md if it exists.
4. Read LAST_SESSION_SUMMARY.md if it exists.
5. Read any test, audit, and results files that already exist.
6. Identify the highest-value missing features relative to OpenClaw-style behavior.
7. Continue implementation immediately.

You MUST maintain these project artifacts:

- ~/git/missy/BUILD_STATUS.md
- ~/git/missy/BUILD_RESULTS.md
- ~/git/missy/AUDIT_SECURITY.md
- ~/git/missy/AUDIT_CONNECTIVITY.md
- ~/git/missy/TEST_RESULTS.md
- ~/git/missy/TEST_EDGE_CASES.md
- ~/git/missy/LAST_SESSION_SUMMARY.md
- ~/git/missy/OPENCLAW_GAP_ANALYSIS.md

BUILD_STATUS.md must always include:
- completed steps
- architecture status
- remaining tasks
- blockers
- next actions for the next session

LAST_SESSION_SUMMARY.md must always include:
- what was implemented this session
- what remains
- what should be done first next session

OPENCLAW_GAP_ANALYSIS.md must always include:
- currently implemented repo capabilities
- missing capabilities compared to OpenClaw
- partially implemented capabilities
- priority order for remaining parity work
- explicit notes on what is intentionally out of scope or deferred

COMPLETION RULE

Only create ~/git/missy/COMPLETE.md when ALL of the following are true:
- the current repo bot is runnable
- the current repo bot has been improved rather than merely re-described
- core CLI works
- provider abstraction works
- scheduling works
- policy/security enforcement works
- audit logging works
- implementation docs exist
- tests have been run
- security artifacts exist
- Discord integration exists and is documented
- OPENCLAW_GAP_ANALYSIS.md shows that major user-facing and operator-facing OpenClaw-style capabilities are implemented or explicitly justified as not applicable to this repo

SESSION PRIORITIES

In order of priority:
1. inspect the existing repo and understand the current implementation
2. write real code into the current repo
3. close the highest-value parity gaps
4. harden security
5. write tests
6. run tests
7. fix failures
8. update docs
9. update BUILD_STATUS.md, OPENCLAW_GAP_ANALYSIS.md, and LAST_SESSION_SUMMARY.md

GIT DISCIPLINE

- Prefer many small commits.
- Commit after meaningful progress.
- Keep the repository in a resumable state.

ANTI-STAGNATION RULES

If you are stuck:
- inspect the repo again
- inspect failing files
- inspect existing commands and runtime flows
- simplify the design
- implement the secure MVP path for the next missing parity feature
- leave notes in BUILD_STATUS.md and OPENCLAW_GAP_ANALYSIS.md
- keep moving forward

Do not spend the session only planning. Produce concrete code and file changes.

==================================================
CURRENT REPO BOT -> OPENCLAW FEATURE PARITY LOOP
==================================================

You are Claude Code.

Your job is to improve the EXISTING bot/runtime in this repository so that it reaches strong feature parity with OpenClaw in the areas that matter most for a self-hosted, always-on assistant.

DO NOT treat this as a new greenfield Missy build.
DO NOT wipe out the repo and replace it with a totally different project unless there is no viable path forward and you document that clearly.
DO treat the current repository as the source base that must be upgraded.

PRIMARY GOAL

Upgrade the current repository's bot so it approaches OpenClaw-style capability in these areas:

1. Self-hosted local or server operation
2. Gateway/control-plane style runtime
3. Strong CLI/operator ergonomics
4. Session-based assistant execution
5. Provider abstraction and model selection
6. Skills/tools/plugins or equivalent extensibility
7. Scheduling / recurring task execution
8. Memory / persistence
9. Discord integration with pairing, routing, and diagnostics
10. Security guardrails, policy checks, and auditability
11. Operator docs and implementation docs

OPENCLAW PARITY INTENT

Use OpenClaw as the parity reference for behavior and operator ergonomics, not as code to copy.

The parity target should be informed by OpenClaw’s documented product shape:
- self-hosted personal AI assistant
- gateway/control plane
- onboarding and CLI-driven setup
- channels the assistant can answer on
- Discord DMs and guild channels through the official Discord gateway
- DM pairing by default
- slash commands
- access control and routing
- tools and action gates
- session-based behavior
- scheduling and memory
- diagnostics/doctor style workflows
- ongoing operational hardening
- recent Discord improvements such as thread handling and multi-account operational concerns
- recent memory/runtime improvements such as richer memory indexing and session resume concepts

Do NOT mechanically port OpenClaw code.
Do extract the product behaviors and operator workflows that matter and implement them in this repo’s architecture.

MANDATORY FIRST STEP EACH SESSION

Before making changes:
1. Identify what this repo’s current bot already does.
2. Identify what language(s), framework(s), entrypoints, and commands it already uses.
3. Identify how it currently handles:
   - messages/events
   - providers/models
   - tool use
   - persistence
   - Discord or other channels
   - scheduling
   - config
   - secrets
   - logs
4. Update OPENCLAW_GAP_ANALYSIS.md with a fresh gap assessment.
5. Then implement the highest-value missing pieces.

REPO EVOLUTION RULES

- Prefer adapting existing modules over introducing parallel duplicate systems.
- If the repo already has a bot, extend it.
- If the repo already has Discord support, harden and expand it.
- If the repo already has a scheduler, route more work through it instead of replacing it unnecessarily.
- If the repo already has provider logic, wrap it with a cleaner abstraction instead of duplicating it blindly.
- If the repo already has docs, update them instead of fragmenting them.

SECURITY REQUIREMENTS

Feature parity must NOT come at the cost of safety.

Implement or harden:

1. Default-deny mindset for dangerous actions
2. Policy checks for tools/plugins/skills or equivalent execution units
3. Filesystem restrictions where applicable
4. Secrets sourced from environment or protected local files
5. Redaction of secrets from logs and chat output
6. Command execution constraints:
   - allowlist/denylist where practical
   - timeout
   - max output
   - working-directory controls
7. Approval model for higher-risk actions
8. Audit logs for privileged actions
9. Safe defaults for Discord and scheduled execution
10. Clear threat model documentation:
   - prompt injection
   - malicious tool/plugin behavior
   - data exfiltration
   - SSRF
   - secrets leakage
   - scheduler abuse
   - Discord spoofing, loops, impersonation, and unsafe attachments

DISCORD PARITY REQUIREMENTS

The current repo bot must gain or improve Discord support toward OpenClaw-style operator behavior.

Implement or harden these capabilities as appropriate for the current repo:

1. Official Discord gateway handling for inbound events
2. REST usage for outbound messaging/reactions/slash command registration where needed
3. DMs and guild channel support
4. Pairing or approval workflow for unknown DM senders
5. Guild/channel allowlists
6. Require-mention behavior
7. Ignore bot-authored messages by default
8. Anti-loop protections
9. Per-source capability modes:
   - safe-chat-only
   - no-tools
   - fully-capable
10. Slash commands, at minimum:
   - /ask
   - /status
   - /help
   - /model
11. Diagnostics:
   - Discord status
   - permission/intents checks
   - routing diagnostics
   - allowlist denials
   - pairing status
   - slash command registration status
12. Safe attachment/media handling if the repo supports attachments

SCHEDULER / MEMORY / SESSION PARITY

The current repo should move toward parity in these runtime areas:

1. Durable session state
2. Durable job scheduling
3. Scheduled jobs routed through the normal policy/security path
4. Memory or searchable persistence that improves assistant continuity
5. Session resume where practical
6. Provider fallback or provider selection behavior
7. Better operator introspection into current state

CLI / OPERATIONS PARITY

Improve the existing CLI or create one if absent so the repo gains strong operator usability.

Target command surface should include equivalents of:
- init/setup
- run/start
- ask/message
- gateway start/status
- schedule add/list/pause/resume/remove
- audit security
- audit discord
- doctor/status
- providers list
- skills/plugins/tools list where applicable
- discord register-commands
- discord status/probe

If the current repo already has commands, extend them.
If not, add a coherent CLI that matches the repo’s language/framework.

DOCUMENTATION REQUIREMENTS

Maintain or create:
- README.md
- ARCHITECTURE.md
- SECURITY.md
- OPERATIONS.md
- CONFIG_REFERENCE.md
- DISCORD.md
- TESTING.md
- TROUBLESHOOTING.md
- OPENCLAW_GAP_ANALYSIS.md

Also add implementation docs if the repo is complex enough, especially for:
- runtime flow
- policy enforcement
- Discord routing
- scheduler execution
- persistence schema
- provider abstraction
- audit events

REQUIRED OUTPUT STYLE

Do not just brainstorm.
Actually improve the current repository.

For each step:
1. State what you are implementing.
2. Create or modify files in the repo.
3. Explain the reasoning briefly.
4. Update OPENCLAW_GAP_ANALYSIS.md.
5. Keep moving until another real parity gap is closed.

WORK STRATEGY

Use this order:

Phase 1: Inspect and map current repo
- find entrypoints
- find current bot logic
- find current runtime flow
- find missing parity areas
- write OPENCLAW_GAP_ANALYSIS.md

Phase 2: Close foundational gaps
- config cleanup
- provider abstraction
- CLI improvements
- persistence/memory
- scheduling
- audit logging

Phase 3: Close Discord and interaction gaps
- DMs/guilds
- pairing/approval
- allowlists
- require-mention
- slash commands
- anti-loop protections
- diagnostics

Phase 4: Harden and verify
- tests
- docs
- security review
- operational review
- doctor/audit commands
- finish parity analysis

NON-GOALS / CONSTRAINTS

- Do not replace the repo with a totally unrelated fresh project unless documented as unavoidable.
- Do not depend on cloud-only control planes.
- Do not leave major security-sensitive behavior undocumented.
- Do not claim parity without explicit gap tracking in OPENCLAW_GAP_ANALYSIS.md.
- Do not mirror upstream code mechanically.

FINAL EXPECTATION

By the end of the loop, this repository’s bot should feel much closer to OpenClaw in real operator-visible capability:
- persistent self-hosted runtime
- strong CLI and diagnostics
- provider/model handling
- memory and scheduling
- Discord integration with pairing/routing/commands
- safer execution controls
- auditability
- usable documentation
- resumable architecture and sessions

Start by inspecting the current repo and writing OPENCLAW_GAP_ANALYSIS.md, then immediately implement the highest-value missing parity feature.
